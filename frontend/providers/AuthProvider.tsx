'use client'

// Phase 0 shell — auth context and session management implemented in Phase 3
// Do not add auth logic here until Phase 3 backend core is complete

export function AuthProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}