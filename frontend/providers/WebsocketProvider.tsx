'use client'

// Phase 0 shell — WebSocket connection management implemented in Phase 4
// Do not implement WebSocket logic here until Phase 4 realtime runtime is complete

export function WebsocketProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}